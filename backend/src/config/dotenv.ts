export const env = {
    BACKEND_PORT: process.env.BACKEND_PORT || 8081,
    FRONTEND_PORT: process.env.FRONTEND_PORT || 4001,
    LOCAL_HOTSPOT_PC: process.env.LOCAL_HOTSPOT_PC || 'localhost',
    LOCAL_HOTSPOT_PHONE: process.env.LOCAL_HOTSPOT_PHONE || 'localhost'
}